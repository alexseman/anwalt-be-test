<!-- HTML for static distribution bundle build -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>API Documentation</title>
    <link rel="stylesheet" href="{{ asset('css/swagger-ui.css') }}">
</head>

<body>
<div id="swagger-ui"></div>

<script src="{{ asset('js/swagger-ui-standalone-preset.js') }}"></script>
<script src="{{ asset('js/swagger-ui-bundle.js') }}"></script>

<script>
    window.onload = function() {
        // Begin Swagger UI call region
        console.log(window.location.pathname);
        const ui = SwaggerUIBundle({
            url: window.location.protocol + "//" + window.location.hostname + "/docs/api/{{ version }}.json",
            dom_id: '#swagger-ui',
            deepLinking: true,
            presets: [
                SwaggerUIBundle.presets.apis,
            ],
        })
        // End Swagger UI call region
        window.ui = ui
    }
</script>
</body>
</html>
